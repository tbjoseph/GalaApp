// The board's shape, shared by the game screen and the new game dialog so the
// grid the numbers are picked on always matches the grid they are played on.
export const BOARD_COLS = 15;
export const BOARD_ROWS = 10;
export const BOARD_TOTAL = BOARD_COLS * BOARD_ROWS;
